name: 'cloud_sandbox'
version: '1.0'

profile: 'cloud_sandbox'

source-paths: ["models"]
analysis-paths: ["analysis"] 
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"


models:
  cloud_sandbox:

      example:
          materialized: view
